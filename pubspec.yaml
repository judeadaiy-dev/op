name: chat_app
description: تطبيق محادثة احترافي بـ Flutter و Supabase
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  provider: ^6.1.2

  # Backend
  supabase_flutter: ^2.5.6
  
  # Firebase - للإشعارات Push Notifications
  firebase_core: ^3.1.1
  firebase_messaging: ^15.0.1
  flutter_local_notifications: ^17.1.2

  # UI & Theme
  google_fonts: ^6.2.1
  glassmorphism: ^3.0.0
  flutter_animate: ^4.5.0

  # Images & Files
  image_picker: ^1.1.2
  cached_network_image: ^3.3.1
  
  # Utils
  intl: ^0.19.0
  timeago: ^3.6.1
  uuid: ^4.4.0
  
  # Permissions - للمايك والكاميرا والتخزين
  permission_handler: ^11.3.1
  
  # Audio Recording - عشان المايك يشتغل
  record: ^5.1.2
  
  # Audio Player - تشغيل الرسائل الصوتية
  just_audio: ^0.9.39
  
  # File Picker - اختيار ملفات
  file_picker: ^8.0.6
  
  # Path - مسارات الملفات
  path_provider: ^2.1.3
  
  # Connectivity - فحص النت
  connectivity_plus: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  
  # توليد الأيقونة بكل المقاسات
  flutter_launcher_icons: ^0.13.1

# إعدادات الأيقونة
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/app_logo.png"
  min_sdk_android: 21
  remove_alpha_ios: true
  adaptive_icon_background: "#1E88E5"
  adaptive_icon_foreground: "assets/images/app_logo.png"

flutter:
  uses-material-design: true

  # الصور
  assets:
    - assets/images/

  # الخطوط
  fonts:
    - family: Cairo
      fonts:
        - asset: assets/fonts/Cairo-Regular.ttf
        - asset: assets/fonts/Cairo-Bold.ttf
          weight: 700
        - asset: assets/fonts/Cairo-SemiBold.ttf
          weight: 600
